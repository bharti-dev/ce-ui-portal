server:
 port: ${PORT:2000}
 
spring:
 cloud:
  config:
   server:
    git:
     uri: ${HOME}/Snapshotviews/git/ce-ui-portal-properties
 autoconfigure:
  exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

logging:
  level:
    ROOT: INFO
    org.spring.cloud: DEBUG